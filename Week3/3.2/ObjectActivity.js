// Object Activity 1
// Below is an object called currentWeather
const currentWeather = {
    weather: "Rain",
    temp: 50,
    sky: "Overcast",
    details: ["Moderate Rain", 45, "Not Severe"]
  };

console.log(
    `The weather is ${currentWeather.weather}. The temperature is ${currentWeather.temp} and the sky is ${currentWeather.sky}`
    );
